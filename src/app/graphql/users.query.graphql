query GetUsers {
  readUsers {
    dateOfBirth
    email
    id
    name
    patronomic
    surname
  }
}

query GetClients{
  readUsers(where: {credentials: {role: {userType: {eq: CLIENT}}}})
  {
    name
    surname
    credentials {
      phoneNumber
    }
  }
}
